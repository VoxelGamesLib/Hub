task createPom() {
    pom {
        project {
            groupId 'com.voxelgameslib'
            artifactId 'Hub'
            version version

            inceptionYear '2015'
            licenses {
                license {
                    name 'MIT'
                    url 'https://github.com/VoxelGamesLib/Hub/blob/master/LICENSE'
                    distribution 'repo'
                }
            }

            issueManagement {
                system 'GitHub Issues'
                url 'https://github.com/VoxelGamesLib/VoxelGamesLibv2/issues'
            }

            scm {
                connection 'scm:git@github.com:VoxelGamesLib/Hub.git'
                developerConnection 'scm:git@github.com:VoxelGamesLib/Hub.git'
                url 'https://github.com/VoxelGamesLib/Hub'
            }
        }
    }.writeTo("pom.xml")
}